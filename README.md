# Vercel Email API (Flask)

Simple Flask API to send emails via SMTP.

## Setup

1. Fill in `.env` with your SMTP details.
2. Run `pip install -r requirements.txt`
3. Run the app with `python app.py`
